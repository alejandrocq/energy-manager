services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TZ: ${TZ:-UTC}
    environment:
      - TZ=${TZ:-UTC}
    volumes:
      - "${CONFIG_PATH:-./config.properties}:/app/config.properties:ro"
    ports:
      - "8000:8000"
    restart: unless-stopped
