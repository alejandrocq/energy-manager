services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        TZ: ${TZ:-UTC}
    volumes:
      - "${CONFIG_PATH:-./config/}:/app/config/:rw"
    ports:
      - "8000:8000"
    restart: unless-stopped
